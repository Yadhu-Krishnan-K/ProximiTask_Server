class User {
    constructor({ name, googleId, email, pass }) {
      // this.id = id;
      this.username = name;
      this.googleId = googleId;
      this.email = email;
      this.password = pass;
    }

  }
  
  export default User;